# AlumniConnect Frontend Settings
# Specify the backend API URL used at build time.
# For local development use the dev backend. Do NOT commit production URLs here.
VITE_API_BASE_URL=http://127.0.0.1:8000
